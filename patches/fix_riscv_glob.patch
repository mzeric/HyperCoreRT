--- a/toolchain/templates/compiler.BUILD
+++ b/toolchain/templates/compiler.BUILD
@@ -39,7 +39,7 @@ filegroup(
     srcs = [
       PREFIX,
       "{}/lib".format(PREFIX),
-    ] + glob(["lib/gcc/{}/*".format(PREFIX)]),
+    ] + glob(["lib/gcc/{}/*".format(PREFIX)], allow_empty = True),
 )

 # libraries, headers and executables.
